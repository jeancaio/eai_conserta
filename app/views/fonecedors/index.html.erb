<p id="notice"><%= notice %></p>

<h1>Fornecedores de serviço</h1>

<table class="table bordered">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Função</th>      
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @fonecedors.each do |fonecedor| %>
      <tr>
        <td><%= fonecedor.nome %></td>
        <td><%= fonecedor.tipo_servico %></td>        
        <td rowspan="1"><%= link_to fonecedor do %>
          <span class='glyphicon glyphicon-zoom-in'></span>
          <% end %>
        </td>
        <td rowspan="1"><%= link_to edit_fonecedor_path(fonecedor) do %>
          <span class='glyphicon glyphicon-edit'></span>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Incluir', new_fonecedor_path %>

